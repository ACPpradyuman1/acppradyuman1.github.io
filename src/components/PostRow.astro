---
interface Props {
  href: string;
  date: string;
  title: string;
  dek?: string;
}

const { href, date, title, dek } = Astro.props;
---

<a href={href} class="post-row">
  <div class="rail">
    <div>{date}</div>
  </div>
  <div>
    <div class="title">{title}</div>
    {dek && <div class="dek">{dek}</div>}
  </div>
</a>
